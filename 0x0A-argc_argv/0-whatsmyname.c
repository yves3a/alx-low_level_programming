#include <stdio.h>
#include "main.h"

/**
 * main - prints the prgram name
 * @argc: the number of command line
 * @argv: the array of string
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);

}
