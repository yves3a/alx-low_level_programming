#include "main.h"
/**
 * _abs - a function that computes a absolute value of interger
 *@n: number
 * Return: returns interger on successful execution
 */
int _abs(int n)
{

	if (n < 0)
	{
		return (n * -1);
	}
	else
		return (n);

}
